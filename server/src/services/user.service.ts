import env from '@/env'
import { User, users } from '@/db/models/User'
import { db } from '@/db'
import jwt from 'jsonwebtoken'

export class UserService {
  
  constructor(){}
  
  async loginInit(){
    
  }
  
  async loginFinish(){
    
  }
  
  async profileSetup(){
    
  }
}